"use client"; import { useEffect,useState } from "react"; import { useRouter } from "next/navigation";
export default function PortalGuard({role,children}:{role:"super_admin"|"staff";children:React.ReactNode}){const router=useRouter();const [ok,setOk]=useState(false);useEffect(()=>{fetch("/api/auth/me").then(async r=>{if(!r.ok)throw 0;const u=await r.json();if(u.role!==role)throw 0;if(role==="staff"&&u.mustChangePassword&&location.pathname!=="/staff/change-password")return router.replace("/staff/change-password");setOk(true)}).catch(()=>router.replace(role==="staff"?"/staff/login":"/admin/login"))},[role,router]);return ok?<>{children}</>:<div className="min-h-screen grid place-items-center bg-mist text-ink-soft">Checking your account…</div>}
