"use client"; import PortalGuard from "@/components/PortalGuard"; import Link from "next/link"; import { usePathname,useRouter } from "next/navigation"; import { auth } from "@/lib/firebase"; import { signOut } from "firebase/auth";
export default function AdminShell({children}:{children:React.ReactNode}){const path=usePathname(),router=useRouter();async function logout(){await fetch("/api/auth/session",{method:"DELETE"});await signOut(auth);router.push("/admin/login")}return <PortalGuard role="super_admin"><div className="min-h-screen bg-mist"><header className="bg-navy-deep text-white sticky top-0 z-40"><div className="max-w-[1200px] mx-auto min-h-20 px-4 sm:px-7 flex flex-wrap items-center justify-between gap-3 py-3"><Link href="/admin/dashboard"><span className="text-xs text-on-blue uppercase tracking-widest font-bold">UpTech</span><strong className="block text-lg">Administration</strong></Link><nav className="flex items-center gap-1 overflow-x-auto max-w-full"><Link href="/admin/dashboard" className={`px-3 py-2 rounded-full font-bold text-sm ${path==="/admin/dashboard"?"bg-white text-navy":"text-on-blue"}`}>Overview</Link><Link href="/admin/resources" className={`px-3 py-2 rounded-full font-bold text-sm ${path.includes("resources")?"bg-white text-navy":"text-on-blue"}`}>Resources</Link><Link href="/admin/staff" className={`px-3 py-2 rounded-full font-bold text-sm ${path.includes("staff")?"bg-white text-navy":"text-on-blue"}`}>Staff</Link><button onClick={logout} className="px-3 py-2 text-sm font-bold text-on-blue">Sign out</button></nav></div></header>{children}</div></PortalGuard>}
